# -*- coding: utf-8 -*-
"""Dirty-Notebook(2).ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1gpzAwvWZzQa6DVyjYkkaY6EW7lpIO-1r
"""

DRIVE <- "/content/drive/MyDrive/thesis/dlnm-pilot"
list.files(DRIVE)

DRIVE <- "/content/drive/MyDrive/thesis/dlnm-pilot"

system(sprintf("cd /content && cp %s/r_library.tar.gz . && tar -xzf r_library.tar.gz", DRIVE))
.libPaths(c("/content/site-library", .libPaths()))

suppressPackageStartupMessages({
  library(dlnm); library(gnm); library(mixmeta); library(splines)
  library(sf); library(data.table); library(exactextractr); library(terra)
  library(ggplot2); library(viridis); library(lubridate)
})

system(sprintf("cd /content && cp %s/saves_pilot_2026-06-03.tar.gz . && tar -xzf saves_pilot_2026-06-03.tar.gz", DRIVE))
load("/content/saves/pilot_session.RData")

cat("Objects restored:", length(ls()), "\n")
cat("Packages live — fread:", exists("fread"), " crossbasis:", exists("crossbasis"), "\n")

needed <- c("sim","temp_mat","truth_factors","Z","da_age_wide",
            "res","red","fit_stage1","reduce_fit","fit_stage2","simulate_counts")
data.frame(object = needed, present = needed %in% ls())

cat("In .RData on disk:\n")
e <- new.env()
load("/content/saves/pilot_session.RData", envir = e)
print(sort(ls(e)))
cat("\ntemp_mat there?", "temp_mat" %in% ls(e),
    "  sim there?", "sim" %in% ls(e), "\n")

cat("Z:        ", paste(dim(Z), collapse=" x "), " | NA:", sum(is.na(Z)), "\n")
cat("true_load:", paste(dim(true_loadings), collapse=" x "), "\n")
cat("truth_fac:", nrow(truth_factors), "rows | DAUID class:", class(truth_factors$DAUID), "\n")
cat("row align: Z rows == truth_factors rows:", nrow(Z) == nrow(truth_factors), "\n")
cat("17 match:  ncol(Z) == nrow(true_loadings):", ncol(Z) == nrow(true_loadings), "\n")

da_daily_temp <- fread(daymet_csv, select = c("DAUID","date","tmax","tmin"))
da_daily_temp[, DAUID := as.character(DAUID)]
da_daily_temp[, tmean_C := (tmax + tmin) / 2]
temp_wide <- dcast(da_daily_temp, DAUID ~ date, value.var = "tmean_C")
temp_wide <- temp_wide[match(truth_factors$DAUID, temp_wide$DAUID), ]
temp_mat  <- as.matrix(temp_wide[, -1])

n_all_na <- sum(rowSums(is.na(temp_mat)) == ncol(temp_mat))
cat("temp_mat:", paste(dim(temp_mat), collapse=" x "), "\n")
cat("rows all-NA (water DAs):", n_all_na, "\n")
cat("expected drop 7694 -> 7682 =", 7694 - 7682, "\n")
cat("matches:", n_all_na == 12, "\n")

dead_rows <- which(rowSums(is.na(temp_mat)) == ncol(temp_mat))
keep      <- setdiff(seq_len(nrow(temp_mat)), dead_rows)

temp_mat      <- temp_mat[keep, ]
truth_factors <- truth_factors[keep, ]
Z             <- Z[keep, ]
n_da          <- length(keep)
truth_factors[, da_idx := .I]

cat("dropped:", length(dead_rows), "\n")
cat("temp_mat:", paste(dim(temp_mat), collapse=" x "),
    " truth_factors:", nrow(truth_factors),
    " Z:", paste(dim(Z), collapse=" x "), "\n")
cat("align (rownames temp_mat == truth_factors$DAUID):",
    all(rownames(temp_mat) == truth_factors$DAUID), "\n")
cat("Z NA:", sum(is.na(Z)), " temp_mat NA:", sum(is.na(temp_mat)),
    " temp range:", paste(round(range(temp_mat), 1), collapse="-"), "\n")

pca_out <- fit_da_pca(Z, truth_factors$DAUID)

emp_load <- pca_out$pca$rotation[, 1:3]
cormat   <- cor(emp_load, true_loadings)

matched <- numeric(3); avail <- 1:3
for (f in 1:3) {
  best <- avail[which.max(abs(cormat[avail, f]))]
  matched[f] <- cormat[best, f]
  avail <- setdiff(avail, best)
}

cat("cor matrix (emp PC x true F):\n"); print(round(cormat, 2))
cat("\nmatched |cor| per factor:", round(abs(matched), 3), "\n")
cat("mean |cor|:", round(mean(abs(matched)), 3),
    " PASS:", mean(abs(matched)) > 0.7, "\n")

"""C3 passes on mean 0.731 but F3 smeared across PC2/PC3, 0.49 forced match, real best is PC2=0.75 stolen by F1. Soft fail. Morning: F3 noise vs loadings, check 16/17 cross-loads"""

perms <- rbind(c(1,2,3), c(1,3,2), c(2,1,3), c(2,3,1), c(3,1,2), c(3,2,1))
score <- function(p) mean(abs(diag(cormat[p, ])))
best_p <- perms[which.max(apply(perms, 1, score)), ]

cat("best permutation (emp PC -> true F):", best_p, "\n")
cat("diag |cor| under best perm:", round(abs(diag(cormat[best_p, ])), 3), "\n")
cat("mean:", round(mean(abs(diag(cormat[best_p, ]))), 3), "\n\n")

cat("full |cor| matrix:\n")
print(round(abs(cormat), 2))
cat("\nper-true-F max |cor| (ignoring assignment):",
    round(apply(abs(cormat), 2, max), 3), "\n")
cat("F3 best & second-best:",
    round(sort(abs(cormat[,3]), decreasing=TRUE)[1:2], 3), "\n")

cat("da_all exists:", exists("da_all"),
    " | dgrf exists:", exists("dgrf"), "\n")
if (exists("da_all")) cat("da_all rows:", nrow(da_all), "\n")

dir.create("/content/statcan", showWarnings = FALSE)

da_zip_url  <- "https://www12.statcan.gc.ca/census-recensement/2021/geo/sip-pis/boundary-limites/files-fichiers/lda_000b21a_e.zip"
da_zip_path <- "/content/statcan/da_boundaries.zip"
da_dir      <- "/content/statcan/da_extracted"
if (!file.exists(da_zip_path)) download.file(da_zip_url, da_zip_path, mode = "wb")
if (!dir.exists(da_dir)) { dir.create(da_dir); unzip(da_zip_path, exdir = da_dir) }

da_all <- sf::st_read("/content/statcan/da_extracted/lda_000b21a_e.shp", quiet = TRUE)
da_all$DAUID <- as.character(da_all$DAUID)

dgrf_zip_url  <- "https://www12.statcan.gc.ca/census-recensement/2021/geo/sip-pis/dguid-idugd/files-fichiers/2021_98260004.zip"
dgrf_zip_path <- "/content/statcan/dgrf.zip"
dgrf_dir      <- "/content/statcan/dgrf_extracted"
if (!file.exists(dgrf_zip_path)) download.file(dgrf_zip_url, dgrf_zip_path, mode = "wb")
if (!dir.exists(dgrf_dir)) { dir.create(dgrf_dir); unzip(dgrf_zip_path, exdir = dgrf_dir) }
dgrf <- data.table::fread("/content/statcan/dgrf_extracted/2021_98260004.csv")

cat("da_all:", nrow(da_all), "rows |", ncol(da_all), "cols\n")
cat("DAUID class:", class(da_all$DAUID), "\n")
cat("dgrf:", nrow(dgrf), "rows |", ncol(dgrf), "cols\n")
cat("CMADGUID col present:", "CMADGUID_RMRIDUGD" %in% names(dgrf), "\n")

cma_dguids <- c(Montreal = "2021S0503462", Vancouver = "2021S0503933")

cma_da_lists <- lapply(cma_dguids, function(dg) {
  db_rows   <- dgrf[CMADGUID_RMRIDUGD == dg]
  da_dguids <- unique(db_rows$DADGUID_ADIDUGD)
  dauids    <- substr(da_dguids, 10, 17)
  list(n_db = nrow(db_rows), dauids = dauids)
})

for (nm in names(cma_da_lists)) {
  d <- cma_da_lists[[nm]]
  cat(sprintf("%-10s DBs: %6d  unique DAs: %5d  prov prefix: %s\n",
              nm, d$n_db, length(d$dauids),
              paste(unique(substr(d$dauids, 1, 2)), collapse=",")))
}

cma_polys <- lapply(names(cma_da_lists), function(nm) {
  dauids <- cma_da_lists[[nm]]$dauids
  poly   <- da_all[da_all$DAUID %in% dauids, ]
  list(poly = poly,
       n = nrow(poly),
       missing = length(setdiff(dauids, poly$DAUID)),
       area = round(sum(poly$LANDAREA), 1))
})
names(cma_polys) <- names(cma_da_lists)

for (nm in names(cma_polys)) {
  p <- cma_polys[[nm]]
  cat(sprintf("%-10s polys: %5d  missing: %d  land area: %8.1f km2\n",
              nm, p$n, p$missing, p$area))
}

profiles <- list(
  Quebec = list(geo = "004_Quebec",
                csv = "98-401-X2021006_English_CSV_data_Quebec.csv"),
  BC     = list(geo = "059_British_Columbia",
                csv = "98-401-X2021006_English_CSV_data_British_Columbia.csv")
)

options(timeout = 1800)
for (nm in names(profiles)) {
  p   <- profiles[[nm]]
  zip <- sprintf("/content/statcan/profile_%s.zip", nm)
  dir <- sprintf("/content/statcan/profile_%s_extracted", nm)
  url <- sprintf("https://www12.statcan.gc.ca/census-recensement/2021/dp-pd/prof/details/download-telecharger/comp/GetFile.cfm?Lang=E&FILETYPE=CSV&GEONO=%s", p$geo)
  if (!file.exists(zip)) download.file(url, zip, mode = "wb")
  if (!dir.exists(dir)) { dir.create(dir); unzip(zip, exdir = dir) }
  profiles[[nm]]$dir <- dir
  csv_path <- file.path(dir, p$csv)
  profiles[[nm]]$csv_path <- csv_path
  cat(sprintf("%-8s zip: %5.0f MB  csv exists: %s  csv: %5.1f GB\n",
              nm, file.info(zip)$size/1024^2,
              file.exists(csv_path),
              if (file.exists(csv_path)) file.info(csv_path)$size/1024^3 else NA))
}

qc_url <- "https://www12.statcan.gc.ca/census-recensement/2021/dp-pd/prof/details/download-telecharger/comp/GetFile.cfm?Lang=E&FILETYPE=CSV&GEONO=004_Quebec"
system(sprintf("curl -sIL '%s' | head -20", qc_url))
cat("---\n")
system(sprintf("curl -sI '%s' | grep -i -E 'content-length|content-type|location|http'", qc_url))

cat("=== StatCan reachable? ===\n")
system("curl -sI --max-time 15 https://www12.statcan.gc.ca 2>&1 | head -5")
cat("=== Google reachable (control)? ===\n")
system("curl -sI --max-time 15 https://www.google.com 2>&1 | head -5")
cat("=== exit code of statcan curl ===\n")
system("curl -s --max-time 15 -o /dev/null -w 'http_code:%{http_code} exit hint via stderr\n' https://www12.statcan.gc.ca")

qc_url <- "https://www12.statcan.gc.ca/census-recensement/2021/dp-pd/prof/details/download-telecharger/comp/GetFile.cfm?Lang=E&FILETYPE=CSV&GEONO=006_Quebec"
system(sprintf("curl -sIL --max-time 20 '%s' | grep -i -E 'HTTP/|content-length|content-type'", qc_url))

zips <- c(Quebec = "98-401-X2021006_Quebec_eng_CSV.zip",
          BC     = "98-401-X2021006_BC_CB_eng_CSV.zip")

profile_csv <- character()
for (nm in names(zips)) {
  zp  <- file.path("/content", zips[nm])
  dir <- sprintf("/content/profile_%s", nm)
  if (!dir.exists(dir)) { dir.create(dir); unzip(zp, exdir = dir) }
  csv <- list.files(dir, pattern = "CSV_data\\.csv$", full.names = TRUE)
  profile_csv[nm] <- csv[1]
  hdr <- readLines(csv[1], n = 1)
  cat(sprintf("%-8s zip: %s | csv: %.0f MB | header OK: %s\n",
              nm, file.exists(zp),
              file.info(csv[1])$size/1024^2,
              all(c("ALT_GEO_CODE","CHARACTERISTIC_ID","C1_COUNT_TOTAL") %in%
                  strsplit(hdr, ",")[[1]] |> trimws() |> gsub('"','',x=_))))
}
cat("\ncsv paths:\n"); print(profile_csv)

for (nm in c("Quebec","BC")) {
  zp <- sprintf("/content/98-401-X2021006_%s_eng_CSV.zip",
                ifelse(nm=="Quebec","Quebec","BC_CB"))
  cat(sprintf("=== %s ===\n", nm))
  cat("exists:", file.exists(zp), " size MB:",
      round(file.info(zp)$size/1024^2, 1), "\n")
  cat("file type:\n"); system(sprintf("file '%s'", zp))
  cat("zip integrity test:\n"); system(sprintf("unzip -t '%s' 2>&1 | tail -5", zp))
  cat("\n")
}

dir.create("/content/saves", showWarnings = FALSE)

saveRDS(cma_da_lists, "/content/saves/mtl_van_da_lists.rds")
saveRDS(cma_polys,    "/content/saves/mtl_van_polys.rds")
saveRDS(cma_dguids,   "/content/saves/mtl_van_dguids.rds")

today   <- format(Sys.Date(), "%Y-%m-%d")
tarball <- sprintf("/content/saves_mtlvan_%s.tar.gz", today)
system(sprintf("cd /content && tar -czf saves_mtlvan_%s.tar.gz saves/", today))

cat("contents:\n"); system(sprintf("tar -tzf %s", tarball))
cat("\ntarball:", tarball, "—",
    round(file.info(tarball)$size/1024^2, 2), "MB\n")
cat("polys still live — MTL:", nrow(cma_polys$Montreal$poly),
    " VAN:", nrow(cma_polys$Vancouver$poly), "\n")

cat("=== what's in /content/saves/ ===\n")
finfo <- file.info(list.files("/content/saves", full.names = TRUE, recursive = TRUE))
finfo$mb <- round(finfo$size/1024^2, 2)
print(finfo[order(-finfo$mb), "mb", drop = FALSE])
cat("\ntotal MB in saves/:", round(sum(finfo$size)/1024^2, 1), "\n")

system("cd /content && tar -czf saves_mtlvan_clean.tar.gz saves/mtl_van_da_lists.rds saves/mtl_van_polys.rds saves/mtl_van_dguids.rds")
finfo <- file.info("/content/saves_mtlvan_clean.tar.gz")
cat("clean tarball:", round(finfo$size/1024^2, 2), "MB\n")
system("tar -tzf /content/saves_mtlvan_clean.tar.gz")
cat("listing printed above (may be blank — size is the real check)\n")